# Client portfolio repo (PUBLIC, for GitHub Pages). Never commit secrets or build junk.
# The anon key is public by design, but keep every .env out of git anyway.

node_modules/
dist/
build/

.env
.env.*
!.env.example

.DS_Store
Thumbs.db

boukrani/
